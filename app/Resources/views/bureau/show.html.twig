{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bureau</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bureau.id }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ bureau.adresse }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bureau.nom }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bureau_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bureau_edit', { 'id': bureau.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}

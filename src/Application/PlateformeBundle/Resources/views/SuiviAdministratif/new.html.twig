<form method="POST" id="suiviAdministratifNewForm" class="suiviAdministratifForm" action="{{ path('application_new_suivi_administratif', { 'id' : beneficiaire.id }) }}" >
    <p style="font-weight: bold;color: #50a60d;font-size: 13px;">Ajout d'un suivi administratif : </p>
    <div>
        <span>{{ form_label(form.statut) }}</span>
        <span colspan="3">{{ form_widget(form.statut, {'attr' : {'class': 'statutSuiviAd'}}) }}</span>
    </div>
    <div>
        <span>{{ form_label(form.detailStatut) }}</span><div style="display:none" class="block_info_chargement"><img class="img_loading" src="{{ asset('images/ajax-loader.gif') }}" /> <span class="message_chargement text-info"><i>Chargement de la liste</i></span></div>
        <span colspan="3">{{ form_widget(form.detailStatut, {'attr' : {'class': 'detailStatutSuiviAd', 'disabled':'disabled'}}) }}</span>
    </div>
    <div>
        <span>{{ form_label(form.info) }}</span>
        <span colspan="3">{{ form_widget(form.info) }}</span>
    </div>
    <div>
        <span class="valider">{{ form_widget(form.submit) }}</span>
    </div>
    <div  class="form-rest">
        <span>{{ form_rest(form) }}</span>
        <span>{{ form_errors(form) }}</span>
    </div>
</form>
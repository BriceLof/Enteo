{%  if beneficiaire.suiviAdministratif is not null %}
            {% for suiviAdministratif in beneficiaire.suiviAdministratif %}
                <div class="showAllSuiviAdministratif">
                    <div class="elementSuivi">{{ suiviAdministratif.date|date('d/M/Y') }}</div>
                    <div class="elementSuivi">{{ suiviAdministratif.quoi }}</div>
                    <div class="elementSuivi">{{ suiviAdministratif.qui }}</div>
                    <div class="elementSuivi glyphiconSuivi">
                        <span style="color: black;margin-left: 10px;margin-right: 10px" data-toggle="modal" data-target="#myModalSuiviAdministratifDelete{{ loop.index }}" class="glyphicon glyphicon-remove"></span>
                    </div>
                    <div  class="modal fade" id="myModalSuiviAdministratifDelete{{ loop.index }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                    <h4 class="modal-title" id="myModalLabel">Supprimer Suivi Administratif</h4>
                                </div>
                                <div class="modal-body">
                                    <div>
                                        <span>Voulez-vous vraiment supprimer ce Suivi Administratif?</span>
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <div>
                                        <button type="button" class="btn btn-default deleteBtn" data-dismiss="modal">Annuler</button>
                                        <a style="width: 95px;float: right" href="{{ path('application_delete_suivi_administratif', { 'id': suiviAdministratif.id }) }}">
                                            <button class="btn btn-danger">Supprimer</button>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>






                    <div class="elementSuivi glyphiconSuivi">
                        <span style="color: black;" data-toggle="modal" data-target="#myModalSuiviAdministratifModify{{ loop.index }}" class="glyphicon glyphicon-pencil"></span>
                    </div>
                    <div class="modal fade" id="myModalSuiviAdministratifModify{{ loop.index }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                        <div class="modal-dialog" role="document">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                        <span aria-hidden="true">&times;</span>
                                    </button>
                                    <h4 class="modal-title" id="myModalLabel">Modifier Suivi Administratif</h4>
                                </div>
                                    <form method="POST" id="suiviAdministratifEditForm" action="{{ path('application_edit_suivi_administratif', {'id': suiviAdministratif.id}) }}" >
                                        {{ render(controller("ApplicationPlateformeBundle:SuiviAdministratif:update",{'id': suiviAdministratif.id})) }}
                                    </form>
                            </div>
                        </div>
                    </div>
                </div>
            {% endfor %}
{%  endif %}